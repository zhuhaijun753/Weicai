11.2日
	更新dtc读写错误，错误记录dtc bug
	更新清除dtc
	更新tim定时器。（定时最大值为6500ms,由于寄存只有16位计数值）
        更新28服务 ，禁止应用报文通信
 	更新检测当前故障码时刻断电 下次上电检测 dtc异常

	更新85禁止record dtc
	sizeof(avm_dtc)有疑问

	更新自诊断 报文超时dtc	

	非默认会话转换 状态复位更新



11.4
	 
  	更新上电CAN初始化就发送0x520，内容为均为0。（代码放在booting循环内）
	更新版本号ver1.0.0
	更新未收到心跳重启不成功
	
11.19	更新上电发送0x520默认值不正确。data内容应全部为 0
	更新休眠唤醒 状态切换 未发送0x520

12.05   未加转义。更新进入休眠之前等待ARM保存现场的3s时间内收到can消息或者ACC状态变化（message.c内accoff函数）

12.06   加入转义。。。
12.07   更新进入ACCOF 不发送报文
12.08 	更新 noack 和 busoff处理
12.10   更新ADC采样校准，更新在9-16V正常工作，低于6.5V不发报文，其它范围ARM关闭，可以发送报文
	更新休眠机制

12.21 优化整个代码结构、框架、底层API接口函数，优化代码size。解决启动或者唤醒时偶尔启动不了ARM

01.03  更新 APP安全算法bug，更新为随机种子  

01.15 登哥：更新usart发送dma缓存深度为1级，dma中断和usart中断更新
	    更新noack处理和busoff处理函数
      徐智：更新电源管理
	    更新dtc
01.16 修改body can 发送方式 满足潍柴规范 （周期发送与事件型发送）


2019.03.8  潍柴诊断测试问题更新（针对2ndTest_Report_AVM报告）